info:
  name: Activity List
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/activities"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
