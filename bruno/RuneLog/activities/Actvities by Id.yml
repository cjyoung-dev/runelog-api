info:
  name: Actvities by Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/activities/test"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
