info:
  name: Player Count
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/players/count"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
